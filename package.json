{
  "name": "node-tests-students",
  "private": true,
  "scripts": {
    "test": "jest --runInBand",
    "test:coverage": "jest --runInBand --coverage",
    "lint": "eslint ./students",
    "lint:fix": "eslint ./students --fix",
    "format": "prettier --check ./students",
    "format:fix": "prettier --write ./students"
  },
  "devDependencies": {
    "eslint": "^9.38.0",
    "jest": "^29",
    "prettier": "^3.6.2"
  }
}
